fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
python-dotenv>=1.0.0
whisperx @ git+https://github.com/m-bain/whisperX.git
# Explicit (also a whisperx transitive dep): first-run provisioning downloads
# the transcription/diarization models via huggingface_hub.snapshot_download.
huggingface_hub>=0.24.0
# Accelerated Xet transfers for the HuggingFace repos above. Without it,
# huggingface_hub logs "Xet Storage is enabled ... falling back to regular HTTP"
# and downloads are markedly slower — the slow path behind first-run stalls.
hf_xet>=1.0.0
torch
torchaudio
# Pinned to match torch 2.8 ABI. Newer torchcodec releases (0.10+) target
# torch 2.10+ and fail to load on macOS with torch 2.8. Requires FFmpeg 4-7
# at runtime (see README — install ffmpeg@7 on macOS).
torchcodec==0.7.0
noisereduce>=3.0.0
pyloudnorm>=0.1.1
soundfile>=0.12.0
praat-parselmouth>=0.4.7

# Dev dependencies
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
pytest-cov>=6.0.0
ruff>=0.9.0
